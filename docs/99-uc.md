# TodoListApp 유스케이스 다이어그램

**참조 문서**: [PRD](./2-prd.md) · [도메인 정의서](./1-domain-definition.md)

---

```mermaid
flowchart LR
    Guest(["👤 비인증 사용자"])
    AuthUser(["👤 인증된 사용자"])
    System(["⚙️ 시스템"])

    subgraph App["TodoListApp 시스템 경계"]

        subgraph auth_area["🔐 인증 관리"]
            uc1(["회원가입"])
            uc2(["로그인"])
            uc3(["로그아웃"])
            uc4(["토큰 재발급"])
            uc5(["개인 정보 수정"])
            uc6(["회원 탈퇴"])
        end

        subgraph todo_area["📋 할일 관리"]
            uc7(["할일 등록"])
            uc8(["할일 목록 조회\n(필터링 · 페이지네이션)"])
            uc9(["할일 상세 조회"])
            uc10(["할일 수정"])
            uc11(["할일 삭제"])
            uc12(["완료 여부 토글"])
        end

        subgraph cat_area["🗂️ 카테고리 관리"]
            uc13(["카테고리 목록 조회"])
            uc14(["카테고리 추가"])
            uc15(["카테고리 수정"])
            uc16(["카테고리 삭제"])
        end

        subgraph sys_area["⚙️ 시스템 자동 처리"]
            uc17(["기본 카테고리 생성\n일반·업무·개인"])
            uc18(["할일 카테고리 이전\n→ 일반 카테고리"])
            uc19(["사용자 데이터\n전체 즉시 삭제"])
        end

    end

    %% 비인증 사용자
    Guest --> uc1
    Guest --> uc2
    Guest --> uc4

    %% 인증된 사용자 — 인증 관리
    AuthUser --> uc3
    AuthUser --> uc5
    AuthUser --> uc6

    %% 인증된 사용자 — 할일 관리
    AuthUser --> uc7
    AuthUser --> uc8
    AuthUser --> uc9
    AuthUser --> uc10
    AuthUser --> uc11
    AuthUser --> uc12

    %% 인증된 사용자 — 카테고리 관리
    AuthUser --> uc13
    AuthUser --> uc14
    AuthUser --> uc15
    AuthUser --> uc16

    %% 시스템 자동 처리 (include 관계)
    uc1  -. "«include»" .-> uc17
    uc16 -. "«include»" .-> uc18
    uc6  -. "«include»" .-> uc19

    System --> uc17
    System --> uc18
    System --> uc19
```

---

## 액터 정의

| 액터 | 설명 |
|------|------|
| **비인증 사용자** | 회원가입 또는 로그인 전 상태. 인증 관련 유스케이스만 접근 가능 |
| **인증된 사용자** | JWT 로그인 완료 상태. 모든 도메인 유스케이스 접근 가능 |
| **시스템** | 사용자 액션에 의해 자동으로 실행되는 내부 처리 주체 |

## 관계 설명

| 관계 | 대상 | 설명 |
|------|------|------|
| `«include»` | 회원가입 → 기본 카테고리 생성 | 가입 성공 시 반드시 실행 |
| `«include»` | 카테고리 삭제 → 할일 카테고리 이전 | 삭제 전 소속 할일을 `일반`으로 이전 후 삭제 |
| `«include»` | 회원 탈퇴 → 데이터 전체 삭제 | 탈퇴 시 모든 할일·카테고리·토큰 즉시 삭제 |
